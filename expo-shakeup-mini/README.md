# ShakeUp Mini Expo Prototype

Interactive React Native/Expo kiosk prototype for testing the member flow.

Run after installing dependencies:

```powershell
cd D:\Chili\ShakeUp\expo-shakeup-mini
npm install
npm run web
```

The flow covers:

- Start screen
- Goal selection
- Flavor selection
- Payment
- Making countdown
- Pickup/reset
